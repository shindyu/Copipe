import Quick
import Nimble

class CopipeTest: QuickSpec {
    override func spec() {
        describe("vc") {
            it("hoge") {
                expect(true).to(beTrue())
            }
        }
    }
}
